import { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";
import { getCourse, getLoggedInStudent } from "../services/api";
function CourseDetails() { const { courseId } = useParams(); const [course, setCourse] = useState(null); const [error, setError] = useState(""); useEffect(() => { getCourse(courseId).then(setCourse).catch((e) => setError(e.message)); }, [courseId]); if (error) return <div className="p-6 text-red-700">Unable to load course: {error}</div>; if (!course) return <div className="p-6 text-slate-500">Loading course...</div>; const applyLink = getLoggedInStudent() ? `/apply/${courseId}` : "/login"; return <div className="p-4 sm:p-6"><Link to="/courses" className="text-sm font-medium text-blue-600">← Back to Courses</Link><section className="mt-5 max-w-2xl rounded-xl border bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">{course.department?.departmentName}</p><h1 className="mt-1 text-2xl font-bold">{course.courseName}</h1><dl className="mt-6 grid gap-5 sm:grid-cols-2"><div><dt className="text-sm text-slate-500">Duration</dt><dd className="mt-1 font-medium">{course.duration || "—"}</dd></div><div><dt className="text-sm text-slate-500">Course Fee</dt><dd className="mt-1 font-medium">₹{course.fees}</dd></div><div className="sm:col-span-2"><dt className="text-sm text-slate-500">Department Description</dt><dd className="mt-1">{course.department?.description || "Contact the department for more programme information."}</dd></div></dl><Link to={applyLink} className="mt-7 inline-block rounded-lg bg-slate-900 px-5 py-3 font-medium text-white">Apply Now</Link>{!getLoggedInStudent() && <p className="mt-2 text-sm text-slate-500">Please sign in before applying.</p>}</section></div>; }
export default CourseDetails;
